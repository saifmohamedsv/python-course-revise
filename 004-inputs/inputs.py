# Take data from the user using the prompt

# This will take user's input and assign it to name variable
name = input("What is your name: ")

print(name)
 

# What if I want to take numbers ?! 🤔 input always takes strings by default
# So, We'll use type casting, we talked about it earlier

age = int(input("What is your age: "))

print(age)


# Float = Decimals = 12.50, 5.25
salary = float(input("What is your salary: "))

print(salary)
